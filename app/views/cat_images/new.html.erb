<!DOCTYPE html>
<html>

  <body>

    <div class="container-fluid">

        <h3 class="text-center">うちねこと～こ～</h3>

          <div class="row">
            <div class="col-2"></div>

            <div class="col-8">

          <div class="d-flex flex-column align-items-center">
            <%= form_for(@cat_image) do |f| %>

              <div class="col">

                <% if @cat_image.errors.any? %>
                  <ul><li><%= @cat_image.errors.count %>件のエラーが発生しました<br></li>
                    <% @cat_image.errors.full_messages.each do |message| %>
                      <li><%= message %></li>

                    <% end %>
                  </ul>
                <% end %>
                <%= attachment_image_tag @cat_image, :image, :fill, 300, 300, format: 'jpeg' %>
                  <%= f.attachment_field :image, placeholder: "猫ちゃん" %>
                  <p>正方形サイズがおすすめです</p>
              </div>

              <div class="col">
                <ul>
                  <li>catname:
                      <%= f.text_field :catname %></li><br>

                  <li>memo:
                      <%= f.text_area :memo %></li><br>

                  <li>tag:
                      <%= f.text_area :tag_list, value: @cat_image.tag_list.join(',') ,class: "form-control",placeholder: "タグ,タグ..." %></li><br>
                  <li>
                      <%= f.submit "新規登録", class:"btn btn-primary btn-block" %></li></ul>
              </div>
              <% end %>
          </div>

            </div>
            <div class="col-2"></div>
          </div>
    </div>

  </body>

</html>