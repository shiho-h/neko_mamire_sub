
<!DOCTYPE html>
<html>

  <body>

    <div class="container-fluid">

        <h3 class="text-center">うちねこへんしゅ～</h3>

          <div class="row">
            <div class="col-4"></div>

            <div class="col-4">

            <div class="row flex-column align-items-center">
              <div class="col-6">
                  <%= attachment_image_tag @cat_image, :image, :size => '150x150' %>
              </div>

              	<%= form_for(@cat_image) do |f| %>
              <div class="col-6">
                      <ul>
                        <li>catname:
                      <%= f.text_field :catname %></li><br>

                      <li>memo:
                      <%= f.text_area :memo %></li><br>
                      <li>tag:
                      <%= f.text_field :tag_list, value: @cat_image.tag_list.join(',') %></li><br>

                    <li><%= f.submit "保存", class:"btn btn-primary btn-block btn-xs" %></li><br>
                    </ul>

              </div>
            </div>

            <% end %>
            <div class="col-4"></div>

          </div>
    </div>
  </body>

</html